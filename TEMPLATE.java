/**
 * [NUMBER]. [TITLE]
 * Difficulty: [Easy | Medium | Hard]
 * Pattern:    [Pattern name matching folder roadmap, e.g. Arrays & Hashing]
 * Link:       https://leetcode.com/problems/[slug]/
 *
 * PROBLEM:
 * Plain-English summary of what you must compute or decide. Include a tiny
 * example with input → output (and brief note if the trick is non-obvious).
 *
 * INTUITION:
 * The “aha” moment: why this idea works, what invariant or observation makes
 * the algorithm correct, and why a simpler attempt fails (e.g. brute force).
 *
 * APPROACH:
 * 1. Step one — setup / data structures
 * 2. Step two — main loop or recurrence
 * 3. Step three — transitions / updates
 * 4. Step four — extract answer / edge cases
 *
 * COMPLEXITY:
 * Time:  O(?) — one sentence tied to n (and other parameters if any)
 * Space: O(?) — auxiliary memory excluding output unless stated otherwise
 */
// import statements as needed

class ClassNameInPascalCase {
    // public method signatures matching LeetCode problem
}
